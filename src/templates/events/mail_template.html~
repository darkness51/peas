MutluGunumuz tarafından bir {{event.name}} etkinliğine davet edildiniz.

Etkinliğin açıklaması:
{event.description}

Etkinliğin tarihi:
{event.start} - {event.end}

Adres:
{event.address}
{event.location}

<a href="{% url event_detail slug=event.slug %}" >{{ event.name }}</a>